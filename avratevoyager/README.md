* clone [AVrateVoyager](https://github.com/Telecommunication-Telemedia-Assessment/AVrateVoyager)
* replace `config.json` in the `app` folder
* place all images of `imgs` in app/stimuli (maybe convert to jpeg)
* override all templates provided in the template folder in `app/template`
